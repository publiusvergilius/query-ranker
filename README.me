# RankerQuery
## Priority queue based on accesses to a query
### Useful to caching the most accessed queries

# Todo
## 1 - Since the access to a query inside the Priority Query is log(n), would be better to firrt store in a map incrementing it's counting accesses, then after a stale time push the list of queries to the priorioty queue 
